# ==============================================================================
# _deprecated/placeholder.yml
#
# v3.0.0-rc5 后可以删除此占位文件。
# 存在意义：保证 _deprecated/ 目录在 git 中存在（不被 .gitignore 过滤）。
# ==============================================================================

# 保留为空 groups，不会在规则加载范围内生效（glob 模式 `*.yml` 不匹配 _* 前缀）。
# 如果某 v2.x 用户误把这个目录加入 rule_files，会因为没有可执行规则触发 warning
# 而不会误激活旧告警。
groups: []
